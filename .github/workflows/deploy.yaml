name: Deploy to Kubernetes Engine
description: Deploys the application to Google Kubernetes Engine (GKE) on push to main branch
on: push

jobs:
  first_job:
    runs-on: ubuntu-latest 

    steps:
      - name: Hello world
        run: echo "Hello, world!"

  
  second_job:
    runs-on: ubuntu-latest

    steps:
      - name: Hello again
        run: echo "Hello again, world!"
